export function handlePerformDidMountHook() {
    /*const {sidebarMenuItems, updateSidebarMenu} = this.props;
    const data = handleActiveRouteMenuItem(sidebarMenuItems);
    updateSidebarMenu(cloneDeep(data));
    this.setState({ mounted : true });*/
}